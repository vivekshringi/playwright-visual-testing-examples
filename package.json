{
  "name": "playwright-example",
  "version": "0.0.1",
  "description": "\"trying out playwright visual regression testing capabilities\"",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "slowTests": "npx playwright test --grep @coffeeMenu",
    "browser": "npx playwright test browser.spec",
    "scroll": "npx playwright test scroll-animation.spec",
    "drag": "npx playwright test drag-and-drop.spec --debug",
    "update-screens": "npx playwright test --update-snapshots"
  },
  "repository": {
    "type": "git",
    "url": "null"
  },
  "keywords": [
    "automation",
    "visual",
    "testing",
    "playwright"
  ],
  "author": "vivek",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/chance": "^1.1.4",
    "@types/node": "^20.8.6"
  },
  "dependencies": {
    "chance": "^1.1.11",
    "fs": "^0.0.1-security"
  }
}
